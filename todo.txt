*IntermissionPanelDemo works
 *replace ALL tweens
  *regression test: MusicPlayer tween should work when 
 *fix warnings/errors
  *static method warning
  *tween warnings
 *reveal a frog card
 *bug: cards aren't centered on the screen
  *IntermissionCards position is not aligned correctly
 *frog dance
  *frogs run out
   *bug: can't cast array to array[string]
   *bug: frogs don't face the proper direction
   *bug: frog running animations do not play (loop is set to false?)
   *bug: frogs move too smoothly; no 'jerky movement'
  *dance song plays
  *frogs dance
   *bug: one odd frame when starting a new dance
  *frogs run away
  *music is restored
 *remove center color rect
 *shark chase works
  *sharks chase
  *eat fingers
 *music
 *hugs
*bug: game scales weird; cards do not grow
*bug: cards have rough edges; filtering settings changed
-LevelRulesDemo works
 -frodoku
 -froggo
 -fruit maze
 -maze
  -unused 'lucky_chance'
  -unused 'remaining_mistakes'
 -pattern memory
 -secret collect
 -word find
-Puzzle works
-Game works
-RunningFrog works
 -'Update Dances' editor toggle works


avoid boolean evaluations like 'if pos_arr'
Replace 'loop = true' for animations with 'loop_mode=1'
replace 'Array.remove()' with 'Array.remove_at()'
replaced 'suppress_warnings' calls with annotations
methods can't call their own static methods; suppress warnings
replace tweens; it's clunky
assigning your own fields now calls your setters
set project's stretch mode to 'canvas_items'


IntermissionCards position: 246, 154
IntermissionCards2 position: 526, 274


bad:
56: position (462, 250) -> (382, 170); get_parent().size() == (924, 500); cell_pos_bounds=[P: (0, 0), S: (1, 1)]
56: position (382, 170) -> (422, 130); get_parent().size() == (924, 500); cell_pos_bounds=[P: (0, 1), S: (0, 0)]

good:
56: position (462, 250) -> (342, 130); get_parent().size() == (924, 500); cell_pos_bounds=(1, 1, 0, 0)
56: position (342, 130) -> (382, 130); get_parent().size() == (924, 500); cell_pos_bounds=(0, 1, 1, 0)

cell_pos position/size are flipped!
